#include "main.h"

/**
 * write_rev_str - writes a reversed string to buffer
 * @list - va_list of arguments
 * @buffr - memory location to write
 * @b_index - index of buffr to start writing
 * Return: number of chars written, -1 if error
 */
int write_rev_str(va_list list, ...)
{
	(void)list;
	printf("Reversed String written...\n");
	return (0);
}
