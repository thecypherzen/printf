#include "main.h"

/**
 * write_rot - writes a rot13 encoded string to a buffer
 * @list - va_list of arguments
 * @buffr - memory location to write
 * @b_index - index of buffr to start writing
 * Return: number of chars written, -1 if error
 */
int write_rot(va_list list, ...)
{
	(void)list;
	printf("Rot 13 String written...\n");
	return (0);
}
